---
import siteData from '../data/dataService';
const { contact } = siteData;
---

<section id="contacto" class="contact">
	<div class="container">
		<div class="contact-card">
			<div class="contact-info">
				<h2>Contacto Profesional</h2>
				<p>¿Tienes alguna consulta? No dudes en contactarme por cualquiera de estos medios.</p>
				
				<div class="info-list">
					<div class="info-item">
						<strong>Email:</strong>
						<span>{contact.email}</span>
					</div>
					<div class="info-item">
						<strong>Teléfono:</strong>
						<span>{contact.phone}</span>
					</div>
					<div class="info-item">
						<strong>Dirección:</strong>
						<span>{contact.address}</span>
					</div>
				</div>

				<div class="social-links">
					{contact.social.linkedin && <a href={contact.social.linkedin} target="_blank">LinkedIn</a>}
					{contact.social.instagram && <a href={contact.social.instagram} target="_blank">Instagram</a>}
				</div>
			</div>
			
			<div class="contact-form-placeholder">
				<h3>Enviame un mensaje</h3>
				<form onsubmit="return false">
					<input type="text" placeholder="Tu Nombre" />
					<input type="email" placeholder="Tu Email" />
					<textarea placeholder="¿En qué puedo ayudarte?"></textarea>
					<button class="btn btn-primary">Enviar Mensaje</button>
				</form>
			</div>
		</div>
	</div>
</section>

<style>
	.contact {
		padding: 8rem 0;
		background: var(--primary-color);
		color: white;
	}

	.contact-card {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 4rem;
		background: rgba(255, 255, 255, 0.05);
		backdrop-filter: blur(10px);
		padding: 4rem;
		border-radius: 30px;
		border: 1px solid rgba(255, 255, 255, 0.1);
	}

	.contact-info h2 {
		color: white;
		font-size: 2.5rem;
		margin-bottom: 1.5rem;
	}

	.info-list {
		margin: 2.5rem 0;
	}

	.info-item {
		margin-bottom: 1.5rem;
		display: flex;
		flex-direction: column;
	}

	.info-item strong {
		color: var(--accent-color);
		font-size: 0.9rem;
		text-transform: uppercase;
	}

	.social-links {
		display: flex;
		gap: 1.5rem;
	}

	.social-links a {
		padding: 0.5rem 1.5rem;
		background: rgba(255, 255, 255, 0.1);
		border-radius: 50px;
		font-size: 0.9rem;
	}

	.social-links a:hover {
		background: var(--accent-color);
		color: var(--primary-color);
	}

	.contact-form-placeholder {
		background: white;
		padding: 3rem;
		border-radius: 20px;
		color: var(--text-color);
	}

	.contact-form-placeholder h3 {
		margin-bottom: 2rem;
	}

	form {
		display: flex;
		flex-direction: column;
		gap: 1.2rem;
	}

	input, textarea {
		padding: 1rem;
		border: 1px solid #ddd;
		border-radius: 10px;
		font-family: var(--font-main);
	}

	textarea {
		height: 120px;
	}

	button {
		width: 100%;
	}

	@media (max-width: 968px) {
		.contact-card {
			grid-template-columns: 1fr;
			padding: 2rem;
		}
	}
</style>
